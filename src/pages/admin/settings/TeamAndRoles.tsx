import TeamMembers from "../TeamMembers";

/**
 * Settings shortcut for Team & Roles
 * Reuses the same component as /admin/team-members
 */
export default function TeamAndRoles() {
  return <TeamMembers />;
}
